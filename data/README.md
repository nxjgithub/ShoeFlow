# data 目录说明

本目录用于存放本地数据索引、运行输出和未来的数据资产。

## 目录约定

- `samples/`：样例素材索引，适合纳入版本管理。
- `outputs/`：CLI 或管线生成的结果，不纳入版本管理。
- `runtime/`：临时运行文件，不纳入版本管理。

原始大文件可以放在 `temp_data/` 或未来的对象存储中，再通过 JSON 索引引用。不要在源码中硬编码绝对路径。
