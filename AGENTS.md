# AGENTS.md

本文档给 Codex 和其他协作代理使用，用来保证后续开发始终围绕项目真实目标推进。

## 项目定位

这是一个面向女鞋电商短视频带货的内容生产与优化系统。它不是单一脚本生成器，也不是全自动视频生成器。当前阶段的核心是建立可解释、可复用、可追溯、可迭代的中间产物体系。

## 当前优先级

优先做：

- 女鞋单品场景
- 60 秒以内短视频
- 爆款视频样本归档
- 视频结构化解析对象
- 内容模板对象
- 商品标准化对象
- 模板适配结果
- 脚本与素材清单输出
- 成片与效果数据回流记录

暂不优先做：

- 全品类泛化
- 复杂直播场景
- 完全无人参与的自动剪辑
- 自动投放
- 过重的基础设施

## 工程约定

- Python 包名使用 `gemeiqi`。
- 源码放在 `src/gemeiqi/`。
- 样例业务数据放在 `examples/fixtures/`。
- 原始素材不要直接散落到源码目录，统一通过 JSON 索引引用。
- 运行期输出写入 `data/outputs/`，该目录不纳入版本管理。
- 临时文件写入 `data/runtime/` 或系统临时目录。
- 新增功能必须先明确输入对象和输出对象。

## 数据原则

每条业务数据都尽量保留追溯信息：

- 来源平台
- 来源 URL 或本地资产路径
- 商品 ID
- 模板 ID
- 脚本 ID
- 版本号
- 人工审核状态
- 生成时间或更新时间

视频解析结果不要只写自然语言总结，应优先沉淀结构化字段。

## 代码原则

- 优先使用标准库搭建基础能力，避免过早引入重依赖。
- 引入外部依赖前先确认它承担的是核心能力还是临时便利。
- 保持模块边界清晰：数据契约、存储、管线、CLI 分开。
- 写测试时优先覆盖数据契约和可追溯关系。
- 不要在没有需求的情况下重构无关文件。

## 常用命令

```powershell
$env:PYTHONPATH = "src"
python -m gemeiqi.cli inspect-samples
python -m gemeiqi.cli validate-fixtures
python -m gemeiqi.cli bootstrap-demo
python -m unittest discover -s tests
```

## 当前模块边界

- `contracts.py`：定义轻量数据契约与校验规则
- `repository.py`：负责 JSON 文件读写
- `pipeline.py`：放置可替换的最小业务流程
- `cli.py`：提供命令行入口
- `paths.py`：统一路径解析

后续接入真实视频处理、OCR、转写或大模型生成时，应保持这些边界，不要把外部服务调用直接散落在 CLI 里。
